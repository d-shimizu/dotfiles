# .tmux.conf - Configuration File for tmux Version 1.8
# Created by: Daisuke SHIMIZU <shimizu.daisuke.sd@gmail.com>

# https://bytebucket.org/ns9tks/tmux-ja/wiki/tmux-ja.html
# - set-option (alias set)
#   Setting Session Option
# - set-window-option (alias setw)
#   Setting Window Option
# - bind-key (alias bind)
#   Setting Key Bind


#
# Prefix Key Setting
#
### Set Prefix Key on Global Session
#set-option -g prefix C-z
### Bind New Prefix Key
#bind-key C-z send-prefix
### Reset Default Prefix Key
#unbind-key C-b


#
# LANG Setting
#
### Set LANG Code
set-option -g utf8 on
set-option -g status-utf8 on
set-window-option -g utf8 on
set-window-option -g status-utf8 on

#
# Repeat Option Setting
#
### Set Repeat Interval 1sec
set-option -g repeat-time 1000


#
# BackScroll History Setting
#
### set 
set-option -g history-limit 100000

#
#
#
### status line
set-option -g status-utf8 on
set-option -g status-interval 5
set-option -g status-bg black
set-option -g status-bg white
set-window-option -g window-status-current-bg blue
set-window-option -g window-status-current-fg white

#set-option -g status-right "#T"
set-option -g status-right "[tmux]#T \"#W\"#F(#I/#P) %Y/%m/%d(%a)%H:%M:%S #"
#set-option -g status-right "[tmux]@#(hostname -s) \"#W\"#F(#I/#P) %Y/%m/%d(%a)%H:%M:%S #"
#set-option -g set-titles-string "@#(hostname | sed 's/\\\\..*//')"
#set-option -g status-bg magenta
#if-shell '[[ "$HOSTNAME" == "trooper" ]]' "set-option -g status-bg cyan"
#if-shell '[[ "${OSTYPE:0:6}" == "darwin" ]]' "set-option -g status-bg green"
#set-option -g status-left "#(~/tmux/tmux-powerline/powerline.sh left)"
#set-option -g status-right '#[bg=yellow, bold]  #(echo $LANG) #[bg=red, bold]  %y/%m/%d(%a)%H:%M:%S  '
#set -g status-right '#[bg=red, bold]  #(echo $LANG)  #[bg=blue]  up  #(~/bin/pc-running-time)  #[bg=green]  (#(~/bin/loadaverage))  Mem  #(~/bin/used-mem)%%  #[bg=red, bold]  %y/%m/%d(%a)%H:%M:%S  '
#set -g status-right '#[bg=red] #T \"#W\"(#I/#P) #(echo $LANG)  %y/%m/%d(%a)%H:%M:%S  '
#set-option -g status-right "[tmux] \"#W\"(#I/#P) @ #H"
set-option -g status-right-length 99
## ウィンドウマネージャ向けのタイトル
#set-option -g set-titles on
#set-option -g set-titles-string "[tmux]#T \"#W\"(#I/#P) @ #H"
set -g status-fg black
set -g status-bg white

### tmux powerline
#set-option -g status on
#set-option -g status-interval 2
#set-option -g status-utf8 on
#set-option -g status-justify "centre"
#set-option -g status-left-length 60
#set-option -g status-right-length 90
#set-option -g status-left "#(~/tmux/tmux-powerline/powerline.sh left)"
#set-option -g status-right "#(~/tmux/tmux-powerline/powerline.sh right)"

set-option -g bell-action any

### enable mouse scroll
set-window-option -g mode-mouse on
set-option -g mouse-select-pane on
set-option -g mouse-select-window on
set-option -g mouse-resize-pane on
set-option -g mode-mouse on
# PuTTYを使う場合は、下記の設定はoffを推奨
#set-option -g mouse-utf8 on
set-option -g mouse-utf8 off

### copy&paste mode vi
set-window-option -g mode-keys vi

#
# Pane Resize Setting
#
### resize pane
bind -r h resize-pane -L 1
bind -r l resize-pane -R 1
bind -r j resize-pane -D 1
bind -r k resize-pane -U 1
bind -r H resize-pane -L 5
bind -r L resize-pane -R 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5

#set-option -g default-command ""
#source-file ~/tmux/.tmux.session

### Synchronize
bind e setw synchronize-panes on
bind E setw synchronize-panes off


#
# Config Reload Setting
#
### Set Reload Config (r)
bind-key r source-file ~/.tmux.conf; display-message "Reload Config!!"
